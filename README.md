# day-3-UniqueMarks
The function should accept an object and produce a new object containing entries with
unique marks. If there are multiple entries with the same mark, the function should
include the entry of the oldest individual.
